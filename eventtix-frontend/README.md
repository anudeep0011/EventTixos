# EventTix Frontend (Next.js)

## Deploy on Vercel

1. Import repo `EventTixos`
2. **Root Directory:** `eventtix-frontend`
3. Framework: Next.js
4. Env: `NEXT_PUBLIC_API_URL=https://YOUR-BACKEND/api`
5. Deploy

Do **not** set root to the monorepo root unless using the root `vercel.json`.
